# Compatibility install file. pyproject.toml is the canonical package metadata.

# Runtime dependencies
pydantic>=2.5.0,<3.0.0
textual>=0.50.0
rich>=13.7.0
openai>=1.10.0
httpx>=0.26.0
pyyaml>=6.0.1

# Development and test dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
